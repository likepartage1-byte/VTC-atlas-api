// Onboarding — design constants for Page 1
// All values are defined here so they can be updated from one place.

// Logo sizing: same height used across all onboarding pages so the logo never jumps
export const ONBOARDING_LOGO_FONT_SIZE = 36;
export const ONBOARDING_LOGO_HEIGHT = 88;

// Page indicator: total dots drawn on Page 1.
// Increase this value as pages are added; the indicator adapts automatically.
export const ONBOARDING_TOTAL_PAGES = 3;
export const ONBOARDING_CURRENT_PAGE = 0;
